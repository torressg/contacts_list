name: lista_contatos
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  image_cropper: ^4.0.1
  image_picker: ^1.0.4
  path_provider: ^2.1.1
  path: ^1.8.3
  gallery_saver: ^2.3.2

flutter:
  uses-material-design: true
